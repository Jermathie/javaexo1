package basic;

public class ArraysCodelab04 {

    public static void main(String[] args) {
        System.out.println(plusOne(12));
        System.out.println(firstElementPlusOne(new int[]{5, 8, 6, 7, 5}));
        System.out.println(elementAtIndexPlusOne(5, new int[]{5, 8, 6, 7, 5, 9}));
    }

    /**
     * Implement this method so that it returns someInteger + 1
     */
    public static int plusOne(int someInteger) {
        return 0;
    }

    /**
     * Implement this method so that it returns the first element in the array + 1
     */
    public static int firstElementPlusOne(int[] array) {
        return 0;
    }

    /**
     * 1. Implement this method so that it returns the element at index index + 1
     * 2. If the index is "out of bounds" (it's either too big or too small) return 0
     */
    public static int elementAtIndexPlusOne(int index, int[] array) {
        return 0;
    }
}
